# Frontend defaults for `vitest` and `vite --mode test`.
# Loaded automatically by Vite. Values here apply to unit/integration test runs.

# Demo is off in tests; MITDataSciencePage tests assert the "demo down" notice.
VITE_ENABLE_EMOTION_DEMO=false
